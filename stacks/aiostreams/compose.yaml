services:
  aiostreams:
    image: ghcr.io/viren070/aiostreams:nightly
    restart: unless-stopped
    env_file:
      - config.env
      - .env
    volumes:
      - aiostreams_data:/app/data
    networks:
      - reverse-proxy

volumes:
  aiostreams_data:

networks:
  reverse-proxy:
    external: true
